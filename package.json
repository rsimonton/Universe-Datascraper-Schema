{
  "name": "datascraper-schema",
  "version": "0.0.9",
  "description": "",
  "license": "UNLICENSED",
  "private": false,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist && tsc -b"
  },
  "author": "Kun Wang",
  "main": "dist/index.js",
  "files": [
    "src/global.d.ts",
    "/dist"
  ],
  "dependencies": {
    "@nestjs/mongoose": "^9.0.2",
    "mongoose": "^6.1.6",
    "mongoose-unique-validator": "^3.0.0",
    "rimraf": "^3.0.2"
  },
  "stableVersion": "0.0.1",
  "devDependencies": {
    "typescript": "^4.5.5"
  }
}
